###############
### Startup ###
###############

# Waybar
exec $waybar

# Notification
exec $mako

# Restore saved brightness
exec light -I

# SwayOSD
exec swayosd-server

# Autotiling
exec_always autotiling

# Polkit
exec /usr/lib/xfce-polkit/xfce-polkit

# Displays
exec kanshi

# Idle
exec swayidle -w \
        timeout 300 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
        timeout 600 'systemctl sleep' \
        before-sleep '$lockS -f'
